{% extends "ApplicationEpostBundle::layout.html.twig" %}

{% block javascripts %}
{{ parent() }}
{% endblock %}

  {% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('bundles/applicationepost/css/sliding.css') }}" type="text/css" media="all"/>
     
 {% endblock %}

{% block content_header '' %}
{% block title %}
Tag
{% endblock %}

{% block content %}
     <br>  
     
     
          <a href="{{ path('tags') }}" class="btn btn-small btn-success">Retour</a>
<table class="table table-striped table-bordered table-hover">
        <tbody>
            <tr>
            
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ entity.slug }}</td>
            </tr>
        </tbody>
    </table>

    
  {#  <li>
        <a href="{{ path('tags_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('tags_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>#}

{% endblock %}
